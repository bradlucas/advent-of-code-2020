(ns advent.day19
  (:require [advent.core :as c]
            [clojure.pprint :as pp]
            [clojure.string :as str]))

(defn read-input [file]
  (let [[rules message] (str/split (c/read-input file #"\n\n")]


        ))



(defn part1 [file]
  0)
